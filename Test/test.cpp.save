#include<bits/stdc++.h>
using namespace std ;
typedef long long int ll;

const ll INF = 1e15;
const ll MOD = 1e9 + 7;
const string ttt= "Test";

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t=1; cin>>t;
    while(t--)
    {   string w1="Ashishgup";
        string w2="FastestFinger";
        ll n,k=0,z=0,m=0;
        cin>>n;
        if(n==1) cout<<w2;
        else if(n%2==1) cout<<w1;
        else
        {
            z=n;
            while(z%2==0)
            {
                z/=2; k++;
            }
            if(z==1&&k>1) cout<<w2;
            else if(z==1) cout<<w1;
            else
            {
                for(ll i=3;i*i<=z;i++)
                {
                    if(z%i==0) m+=2;
                }
                if(m==0) m++;
                if(m>1)
                {
                    if(k>1) cout<<w1;
                    else cout<<w2;
                }
                else
                {
                    if(k>1)
                }
            }
        }
        cout<<endl;
    }
    return 0;
}
