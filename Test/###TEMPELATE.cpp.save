#include<bits/stdc++.h>
using namespace std ;
typedef long long unsigned int ll;

const ll INF = 1e15;
const ll MOD = 1e9 + 7;
const string ttt= "Test";

ll fpow(ll a, ll b)
{
    ll res=1;
    while (b>0)
    {
        if (b&1) res=(res*a);
        b=b>>1;
        a=(a*a);
    }
    return res;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t=1; //cin>>t;
    while(t--)
    {
        ll n,k,z,a=0;
        cin>>n;
        k=pow(n,0.1);
        z=fpow(k,10);
        while(z<=n)
        {
            z/=k;
            z*=(k+1);
            a++;
        }
        a--;
        ll b=10-a;
        char arr[]
        for(ll i=0;i<a;i++)
        {
            for(ll i=0;i<n;i++)
        }
    }
    return 0;
}
